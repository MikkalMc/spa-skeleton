const chai =  require('chai');
const expect = chai.expect;

// @todo tests
describe('API Request', function() {
	describe('Get Post', function() {
		it('gets json containing post data', function() {
			expect(1).to.equal(1);
		})
	})
})